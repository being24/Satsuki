services:
  satsuki:
    image: "ghcr.io/being24/satsuki:latest"
    volumes:
      - ./data:/workspaces/satsuki/data
      - ./log:/workspaces/satsuki/log
    env_file:
      - ./.env
    restart: always
